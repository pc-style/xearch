# anti-slop

Source: [dmmulroy/anti-slop](https://github.com/dmmulroy/anti-slop), commit `c44ef22ca116d0ba62a3ff663a0bd13a3f3fa40b` (2026-09-10).

Copied from `skills/install-anti-slop/assets/anti-slop/` at that commit (verified byte-identical with `diff -r`) by the skill's `scripts/install.mjs`.

Installed paths:

- `tools/oxlint/anti-slop/index.ts`: generic plugin (`anti-slop`)
- `tools/oxlint/anti-slop/effect/index.ts`: Effect plugin (`anti-slop-effect`), enabled because `effect` is a direct dependency

`@oxlint/plugins` is pinned to `1.82.0` to match `oxlint`.

Local deviations: none.
